\newcommand{\ToolName}{page-length storage}
\newcommand{\WebDataSet}{Alexa 10k}
% The number of storage policies tested.  I think this is 5: chromium,
% just blocking 3p cookies, split-key (safari), blocking all 3p state, and
% the ephemeral / \ToolName{} approach.
\newcommand{\NumStoragePolicies}{5}
\newcommand{\IFrame}{\texttt{<iframe>}}
\newcommand{\IFrames}{\texttt{<iframe>s}}